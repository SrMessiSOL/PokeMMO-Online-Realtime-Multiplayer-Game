# Tuxemon data generator

Run from repository root:

```bash
node tools/generate-tuxemon-data.js
```

This script generates normalized, deterministic JSON bundles in `client/src/data/tuxemon/` from the local `tuxemon/` content tree.
